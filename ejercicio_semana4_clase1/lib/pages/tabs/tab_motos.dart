import 'package:flutter/material.dart';

class TabMotos extends StatelessWidget {
  const TabMotos({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Motos',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
