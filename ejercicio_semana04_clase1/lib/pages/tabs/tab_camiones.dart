import 'package:flutter/material.dart';

class TabCamiones extends StatelessWidget {
  const TabCamiones({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Camiones',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}
